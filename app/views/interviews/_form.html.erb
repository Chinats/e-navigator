<%= form_with(model: interview, local: true) do |form| %>
  <% if interview.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(interview.errors.count, "error") %> prohibited this interview from being saved:</h2>

      <ul>
      <% interview.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :candidate %>
    <%= form.text_field :candidate, id: :interview_candidate %>
  </div>

  <div class="field">
    <%= form.label :approval %>
    <%= form.text_field :approval, id: :interview_approval %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
